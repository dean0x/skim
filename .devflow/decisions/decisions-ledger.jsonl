{"id":"obs_jk7n2w","type":"decision","pattern":"Fix all noticed issues immediately regardless of scope — 'if you see something, do something'","details":"Fix all noticed issues immediately regardless of scope — 'if you see something, do something'","anchor_id":"ADR-001","decisions_status":"Accepted","status":"created","raw_body":"\n## ADR-001: Fix all noticed issues immediately regardless of scope — 'if you see something, do something'\n\n- **Date**: 2026-04-30\n- **Status**: Accepted\n- **Context**: reviewer surfaced 7 findings including 4 classified as 'follow-up tickets' non-blocking for merge\n- **Decision**: resolve all findings immediately — no deferral to future tickets or tech debt backlog regardless of blocking severity\n- **Consequences**: 'I really hate tech debt' and 'I don't want to leave anything for later' — user treats noticed issues as immediate obligations regardless of blocking status. **Why:** the cost of leaving a known issue is higher than the cost of extended scope. **How to apply:** when reviewers flag any issues (blocking OR non-blocking), always surface them all to the user for decision rather than silently classifying as deferred. The user will almost always choose to fix now.\n- **Source**: self-learning:obs_jk7n2w\n","date":"2026-04-30"}
{"id":"obs_migrated_adr_002","type":"decision","pattern":"Progress trackers and prioritized issue queues belong as GitHub issues, not memory files","details":"Progress trackers and prioritized issue queues belong as GitHub issues, not memory files","anchor_id":"ADR-002","decisions_status":"Accepted","status":"created","raw_body":"\n## ADR-002: Progress trackers and prioritized issue queues belong as GitHub issues, not memory files\n\n- **Date**: 2026-06-03\n- **Status**: Accepted\n- **Context**: After completing issue triage and agreeing on the next 5 priorities, a progress tracker was created in WORKING-MEMORY.md. The user immediately rejected this, noting memory files don't age well.\n- **Decision**: Any prioritized work queue, roadmap tracker, or multi-issue progress tracker must be created as a GitHub issue (e.g., issue #268 \"Roadmap: Next 5 issues\"), not in WORKING-MEMORY.md or any devflow memory file.\n- **Consequences**: GitHub issues survive context compaction, are version-controlled, and are visible to all collaborators. Memory files are session-scoped and lose fidelity over time. **How to apply:** when the user asks to \"track\" multiple upcoming issues or create a work queue, default to `gh issue create` — not a memory file edit. WORKING-MEMORY.md should only reflect current session state (branch, latest commit, blockers), not a durable to-do list.\n- **Source**: sidecar:decisions.2fea848d-a8f7-46da-aa16-be990ed4d829\n","date":"2026-06-03"}
{"id":"obs_a16x3g","type":"decision","pattern":"Replace empirically-baseless acceptance criteria with grounded regression guards rather than chasing impossible targets","confidence":0.95,"observations":1,"first_seen":"2026-06-04T21:26:04Z","last_seen":"2026-06-04T21:26:04Z","status":"created","evidence":["session 0f82ead3: issue #194 A16 specified index < 5% of source; AST structural n-grams produce O(vocab x files) dense postings, making 5% structurally impossible","searched all prior wave research: 5% had no empirical basis and the lexical index A16 mirrors has no size test at all","commit 0ee9de4: replaced assert(ratio < 0.05) with assert(ratio < 3.0); measured 1.23x on 1000-file corpus vs ~3-5x industry norm (Zoekt/Sourcegraph uncompressed trigram)","on-disk compression follow-up (delta+VarInt/Roaring) filed as #273"],"details":"context: issue #194 acceptance criterion A16 demanded the AST index be < 5% of source bytes, but structural AST n-grams are dense by design (O(vocab x files) posting entries) so 5% is unreachable and the figure had no empirical origin in any prior wave research; decision: replace the 5% target with a defensible < 3x source-bytes regression guard (measured 1.23x) as a real non-ignored test, and file on-disk compression as a tracked follow-up (#273); rationale: a regression guard grounded in measurement and industry norms (uncompressed code-search trigram indexes run 3-5x) protects against real bloat, whereas an impossible target either blocks the PR or gets silently ignored; amending issue acceptance criteria is preferable to gaming them","quality_ok":true,"anchor_id":"ADR-003","decisions_status":"Accepted","raw_body":"\n## ADR-003: Replace empirically-baseless acceptance criteria with grounded regression guards rather than chasing impossible targets\n\n- **Date**: 2026-06-04\n- **Status**: Accepted\n- **Context**: issue #194 acceptance criterion A16 demanded the AST index be < 5% of source bytes, but structural AST n-grams are dense by design (O(vocab x files) posting entries) so 5% is unreachable and the figure had no empirical origin in any prior wave research\n- **Decision**: replace the 5% target with a defensible < 3x source-bytes regression guard (measured 1.23x) as a real non-ignored test, and file on-disk compression as a tracked follow-up (#273)\n- **Consequences**: a regression guard grounded in measurement and industry norms (uncompressed code-search trigram indexes run 3-5x) protects against real bloat, whereas an impossible target either blocks the PR or gets silently ignored\n- **Source**: self-learning:obs_a16x3g\n","date":"2026-06-04"}
{"id":"obs_tk3f9w","type":"decision","pattern":"File follow-up and integration tickets up front as Step 0 (before any code), never post-merge, and never leave #NEW placeholder numbers in source","confidence":0.95,"observations":1,"first_seen":"2026-06-07T11:30:01Z","last_seen":"2026-06-07T11:30:01Z","status":"created","evidence":["session: plan reordered so follow-up ticket is filed first in Step 0 before implementation, so its real issue number is hardcoded into the single-node error string during implementation — the #NEW placeholder is removed entirely","session: #198/#199 annotated up front with the contracts they consume (search_ast for Wave 4 intersection; SearchLayer/open()/--list-patterns for the CLI) so integration dependencies are tracked before code","session: user saved this as a standing preference — file follow-up/integration tickets up front, never post-merge, no placeholder numbers in code — to apply by default in future work","user MEMORY.md references feedback_file_tickets_upfront.md as a durable preference, confirming recurrence"],"details":"context: implementation plans repeatedly deferred follow-up/integration ticket filing until after merge, and used #NEW placeholders in source code for not-yet-filed issue numbers, creating a fix-it-later trap where the placeholder is forgotten and the real number is never wired in; decision: front-load all ticketing as an explicit Step 0 that runs before any code is written — file the follow-up ticket first so its real number can be hardcoded directly into code, and annotate consuming/integration issues up front with the contracts they depend on; never use #NEW or any placeholder issue number in committed source; rationale: a placeholder in code is a silent debt that survives the PR and is easily forgotten; filing the ticket first makes the real number available at implementation time, and naming the integration dependencies up front gives pre-merge work a tracked home so it cannot be lost between waves","quality_ok":true,"mayBeStale":true,"staleReason":"code-ref-missing:MEMORY.md","anchor_id":"ADR-004","decisions_status":"Accepted","raw_body":"\n## ADR-004: File follow-up and integration tickets up front as Step 0 (before any code), never post-merge, and never leave #NEW placeholder numbers in source\n\n- **Date**: 2026-06-07\n- **Status**: Accepted\n- **Context**: implementation plans repeatedly deferred follow-up and integration ticket filing until after merge, and used #NEW placeholders in source for not-yet-filed issue numbers, creating a fix-it-later trap where the placeholder survives the PR and the real number is never wired in\n- **Decision**: front-load all ticketing as an explicit Step 0 that runs before any code is written — file the follow-up ticket first so its real issue number can be hardcoded directly into code (e.g. into an error string), and annotate consuming/integration issues up front with the contracts they depend on\n- **Consequences**: a placeholder in code is silent debt that is easily forgotten\n- **Source**: self-learning:obs_tk3f9w\n","date":"2026-06-07"}
{"id":"obs_mrg7vk","type":"decision","pattern":"Never auto-merge a green PR — hand off for the user to explicitly request the squash-merge once CI passes","confidence":0.85,"observations":1,"first_seen":"2026-06-07T16:15:00Z","last_seen":"2026-06-07T16:15:00Z","status":"created","evidence":["session c7d4ee7c: agent reports CI green across all phases yet explicitly states it is NOT auto-merging, citing the user established pattern of explicitly requesting the squash-merge themselves once CI is green","session c7d4ee7c: agent posture is to report the final verdict and hand off for merge rather than performing the merge automatically"],"details":"context: a feature PR (#284) reached green CI through the full implement/validate/scrutinize/align/QA pipeline and the agent had standing capability to merge it; decision: the agent must not auto-merge even when CI is fully green — merge is a user-gated action; the agent reports the final verdict and hands off, and the user explicitly requests the squash-merge themselves; rationale: the user keeps the merge decision as a deliberate human gate so the final integration step is never taken without explicit human intent, even when every automated check has passed","quality_ok":true,"anchor_id":"ADR-005","decisions_status":"Accepted","raw_body":"\n## ADR-005: Never auto-merge a green PR — hand off for the user to explicitly request the squash-merge once CI passes\n\n- **Date**: 2026-06-07\n- **Status**: Accepted\n- **Context**: a feature PR (#284) reached green CI through the full implement/validate/scrutinize/align/QA pipeline and the agent had the standing capability to merge it, yet the agent declined to merge automatically\n- **Decision**: the agent must not auto-merge even when CI is fully green — merge is a user-gated action\n- **Consequences**: the user keeps the merge decision as a deliberate human gate so the final integration step is never taken without explicit human intent, even when every automated check has passed. How to apply: after CI goes green, report the verdict and stop — do not run gh pr merge or any merge command until the user explicitly asks for the squash-merge.\n- **Source**: self-learning:obs_mrg7vk\n","date":"2026-06-07"}
{"id":"obs_fdz9k3","type":"decision","pattern":"In a dual-index build, an unrecoverable per-file desync must abort BEFORE persisting the manifest so the old index survives and the next query self-heals via full rebuild — never silently continue past it","confidence":0.95,"observations":1,"first_seen":"2026-06-09T07:26:51Z","last_seen":"2026-06-09T07:26:51Z","status":"created","evidence":["session c7d4ee7c: Phase 5 Scrutinize of PR #291 (#199 --ast CLI) found a P0 FileId-desync silent-corruption bug in the dual-index consume loop","commit 3aaa99f index.rs: add_file_ngrams rejecting an AST entry AFTER the lexical entry for the same FileId was accepted previously only logged under SKIM_DEBUG and continued, advancing next_file_id and cascading a silent FileId desync into a committed-but-corrupt index","commit 3aaa99f: consume() signature changed from ConsumeResult to anyhow::Result<ConsumeResult>; the AST-add error now returns Err that propagates through run() BEFORE new_manifest.save(), so the old manifest survives and the next query rebuilds from scratch","honors the documented FileId-alignment invariant: lexical and AST indexes share one FileId per file and must advance in lockstep; commit ordering guarantees save() is never reached on a desynced build"],"details":"context: the search index build pipeline indexes each file into BOTH a lexical and an AST index under a shared FileId, then persists a manifest; the consume loop added the lexical entry first, then the AST entry — and on AST add_file_ngrams failure it only logged under SKIM_DEBUG and continued, which advanced next_file_id and silently desynced the two indexes, then still ran build()+save(), committing a CRC-valid-but-corrupt index; decision: treat an AST-add failure that occurs after the same-FileId lexical entry was already accepted as unrecoverable — propagate an error up through run() so it aborts BEFORE new_manifest.save() is ever reached; rationale: persisting a desynced manifest commits silent corruption that survives across sessions, whereas aborting leaves the prior valid manifest in place so the next query self-heals via a full rebuild; the fix encodes the commit-ordering invariant (no save on a known-desynced build) rather than a best-effort fail-soft continue, applying the fail-loud principle to the dual-index commit boundary","quality_ok":true,"anchor_id":"ADR-006","decisions_status":"Accepted","raw_body":"\n## ADR-006: In a dual-index build, an unrecoverable per-file desync must abort BEFORE persisting the manifest so the old index survives and the next query self-heals via full rebuild — never silently continue past it\n\n- **Date**: 2026-06-09\n- **Status**: Accepted\n- **Context**: the search index build pipeline indexes each file into BOTH a lexical and an AST index under a shared FileId, then persists a manifest\n- **Decision**: treat an AST-add failure that occurs after the same-FileId lexical entry was already accepted as unrecoverable — propagate an error up through run() so it aborts BEFORE new_manifest.save() is ever reached\n- **Consequences**: persisting a desynced manifest commits silent corruption that survives across sessions, whereas aborting leaves the prior valid manifest in place so the next query self-heals via a full rebuild\n- **Source**: self-learning:obs_fdz9k3\n","date":"2026-06-09"}
{"id":"obs_qt2m8p","type":"pitfall","pattern":"Post-release verification checklist omitting Homebrew tap check","details":"Post-release verification checklist omitting Homebrew tap check","anchor_id":"PF-001","decisions_status":"Active","status":"created","raw_body":"\n## PF-001: Post-release verification checklist omitting Homebrew tap check\n\n- **Area**: post-release verification checklist (release process documentation in CLAUDE.md)\n- **Issue**: after completing a release the assistant declared 'All done' without proactively checking whether the Homebrew tap PR was merged and the formula updated — the user had to ask explicitly\n- **Impact**: User had to prompt for the brew tap check after the assistant declared the release complete — a broken or lagging Homebrew tap would go unnoticed if the agent treats the checklist as done\n- **Resolution**: Always include `brew update && brew info dean0x/tap/skim` as the fourth post-release verification step. Full checklist per CLAUDE.md: (1) `cargo install rskim` — shows new version, (2) `npx rskim --version` — shows new version, (3) GitHub Release page — 7 binary assets attached, (4) `brew update && brew info dean0x/tap/skim` — formula updated.\n- **Status**: Active\n- **Source**: self-learning:obs_qt2m8p\n"}
{"id":"obs_nk8w2v","type":"pitfall","pattern":"Classifying review findings as pre-existing or deferred to skip resolution","details":"Classifying review findings as pre-existing or deferred to skip resolution","anchor_id":"PF-002","decisions_status":"Active","status":"created","raw_body":"\n## PF-002: Classifying review findings as pre-existing or deferred to skip resolution\n\n- **Area**: review resolution system (Resolver agent, resolution summary output)\n- **Issue**: The assistant grouped unresolved review findings into 'pre-existing', 'deferred', 'tech debt', and 'suggestions' categories and declared resolution complete — implicitly treating these as skippable without explicit user sign-off. The user rejected this framing in two distinct sessions, each time asking to have the skipped items explained and fixed\n- **Impact**: Silently closing pre-existing or tech-debt findings accumulates hidden debt and triggers a correction turn where the user must ask for re-explanation and re-planning\n- **Resolution**: Never present pre-existing, deferred, or tech-debt findings as closed without explicit user sign-off. Surface all findings with explanations and let the user decide. User's explicit policy: 'I am not one to notice an issue and skip it, even if it is preexisting' and 'I really hate tech debt. my approach is if you see something do something.'\n- **Status**: Active\n- **Source**: self-learning:obs_nk8w2v\n"}
{"id":"obs_yw3m6d","type":"pitfall","pattern":"Attributing command failures to external tools without verifying skim's rewrite hook intercepted the command","details":"Attributing command failures to external tools without verifying skim's rewrite hook intercepted the command","anchor_id":"PF-003","decisions_status":"Active","status":"created","raw_body":"\n## PF-003: Attributing command failures to external tools without verifying skim's rewrite hook intercepted the command\n\n- **Area**: rewrite hook rule matching for git commands\n- **Issue**: assistant attributed two git command failures (zsh parenthesis glob expansion in commit message, git branch -d squash-merge ancestry check) to non-skim causes without ruling out whether skim's rewrite hook intercepted those commands\n- **Impact**: genuine skim involvement is dismissed prematurely, causing the user to challenge with 'Are you one hundred percent sure?' — mirrors prior npx instance where zero output was attributed to external npx hanging rather than skim's vitest rewrite\n- **Resolution**: before attributing any command failure to an external tool in a project where skim is installed, verify (1) whether skim's rewrite hook matched that command and (2) whether the rewrite itself could have caused the failure mode. Never declare 'not related to skim' without checking the engine.rs rule table for the failing command pattern.\n- **Status**: Active\n- **Source**: self-learning:obs_yw3m6d\n"}
{"id":"obs_kp2v7n","type":"pitfall","pattern":"u16 depth arithmetic overflow: widening to u32 required before adding offset in depth comparisons","confidence":0.95,"observations":2,"first_seen":"2026-06-03T15:32:38Z","last_seen":"2026-06-04T09:38:58Z","status":"created","evidence":["PR #269 fix commit 4800fa6: gap-fill condition changed from `node.depth > p + 1` (u16) to `u32::from(node.depth) > u32::from(p) + 1` to prevent silent wrap when p == u16::MAX","extract_tests.rs test u16_max_depth_no_panic_no_spurious_null locks the regression","session d85fd9fd: user explicitly confirmed PF-004 as a valid, generally applicable pitfall and directed restoration via decisions-append plumbing","PR #269 commit 4800fa6: gap-fill condition changed from node.depth > p + 1 (u16) to u32::from(node.depth) > u32::from(p) + 1","session d85fd9fd: PF-004 restored atomically via decisions-append so numbering and TL;DR were set correctly"],"details":"area: extract.rs gap-fill depth arithmetic in extract_ast_ngrams_with_weights; issue: the gap-fill condition `node.depth > p + 1` uses u16 arithmetic — when prev_depth p == u16::MAX the addition wraps to 0, making the condition silently false and skipping gap-fill entirely, potentially causing a panic on the subsequent slice index or emitting a spurious edge; impact: a single file with a legitimate CST node at depth 65535 (or corrupt u16 in synthetic input) would bypass the gap-fill guard; resolution: always widen u16 depth values to u32 (or larger) before arithmetic in comparisons — use `u32::from(p) + 1` not `p + 1` when p is u16","quality_ok":true,"staleResolved":"2026-06-04: verified at crates/rskim-search/src/ast_index/extract.rs:160 — u32::from(node.depth) > u32::from(p) + 1 guard present; false positive from bare-filename resolver","mayBeStale":true,"staleReason":"code-ref-missing:extract.rs","anchor_id":"PF-004","decisions_status":"Active","raw_body":"\n## PF-004: u16 depth arithmetic overflow: widen to u32 before adding an offset in depth comparisons\n\n- **Area**: extract.rs gap-fill depth arithmetic in extract_ast_ngrams_with_weights\n- **Issue**: the condition `node.depth > p + 1` uses u16 arithmetic, so when prev_depth p == u16::MAX the addition wraps to 0, making the condition silently false and skipping gap-fill entirely, risking a panic on the subsequent slice index or a spurious parent-child edge\n- **Impact**: a file with a CST node at depth 65535 (or a corrupt u16 in synthetic DI input) bypasses the gap-fill guard\n- **Resolution**: always widen u16 depth values to u32 before arithmetic in comparisons -- use u32::from(p) + 1, not p + 1, when p is u16. Generalizes to any bounded integer: widen before adding an offset rather than risk wrap at the type maximum.\n- **Status**: Active\n- **Source**: self-learning:obs_kp2v7n\n"}
{"id":"obs_acqv8m","type":"pitfall","pattern":"Acceptance criteria copied verbatim from an issue may be empirically baseless — verify against research before treating as a hard gate","confidence":0.9,"observations":1,"first_seen":"2026-06-04T21:26:14Z","last_seen":"2026-06-04T21:26:14Z","status":"created","evidence":["session 0f82ead3: A16 (index < 5% of source) came verbatim from issue #194 but was structurally impossible for dense AST n-gram postings","searching all prior wave research showed the 5% number had no measured basis and the sibling lexical index it mirrors carries no size assertion at all","resolution was to amend the criterion to a measured < 3x guard rather than ignore-tag or game the test"],"details":"area: acceptance criteria / quality gates for index size; issue: a numeric acceptance criterion (A16: index < 5% of source) was inherited verbatim from the GitHub issue without empirical grounding; it was structurally unachievable for the data structure being built and traced to no benchmark in any prior research wave; impact: an impossible gate forces either a blocked PR, an #[ignore] cop-out, or test-gaming — all of which erode trust in the suite; resolution: before enforcing an inherited numeric criterion, trace it to a measured basis; if none exists, replace it with a grounded regression guard (measure the real ratio, bound it against industry norms) and amend the issue to keep acceptance criteria honest","quality_ok":true,"anchor_id":"PF-005","decisions_status":"Active","raw_body":"\n## PF-005: Acceptance criteria copied verbatim from an issue may be empirically baseless — verify against research before treating as a hard gate\n\n- **Area**: acceptance criteria / quality gates for index size\n- **Issue**: a numeric acceptance criterion (A16: index < 5% of source) was inherited verbatim from the GitHub issue without empirical grounding\n- **Impact**: an impossible gate forces either a blocked PR, an #[ignore] cop-out, or test-gaming — all of which erode trust in the suite\n- **Resolution**: before enforcing an inherited numeric criterion, trace it to a measured basis\n- **Status**: Active\n- **Source**: self-learning:obs_acqv8m\n"}
{"id":"obs_dsp4kn","type":"pitfall","pattern":"A subcommand-dispatch guard that requires one flag to be absent silently drops a help-advertised flag combination by falling through to a different code path","confidence":0.9,"observations":1,"first_seen":"2026-06-09T10:10:32Z","last_seen":"2026-06-09T10:10:32Z","status":"created","evidence":["session c7d4ee7c: PR #291 (#199 --ast CLI) review found --ast + --blast-radius with no text query silently dropped --ast by falling through to run_temporal_standalone, which never applies the AST filter","commit b1c143a: standalone --ast dispatch arm previously guarded by blast_radius.is_none(); widened to match --ast regardless of --blast-radius (crates/rskim/src/cmd/search/mod.rs:108-122)","help text advertised --ast + --blast-radius (AST INTERSECT co-change) as a valid composition, so a documented-as-supported feature was silently inert","fix added a guard regression test so the combo dispatches to run_ast_standalone"],"details":"area: subcommand flag dispatch (rskim search empty-query action selection); issue: the standalone --ast dispatch arm was gated by blast_radius.is_none(), so when both --ast and --blast-radius were set with no text query the match fell through to the next arm (run_temporal_standalone) which applies only the co-change filter and silently ignores --ast — even though help text advertises the combination as valid (AST INTERSECT co-change); impact: a feature documented as supported was silently inert with no error, the worst failure mode — the user gets plausible-looking results that omit the requested AST filter; resolution: when a dispatch arm selects on a flag, never use a sibling-flag-absent guard (other_flag.is_none()) to disambiguate composable flags — match the primary flag unconditionally and let the arm body honor the secondary flag; cross-check every advertised flag combination against the actual dispatch arms so no help-documented combo can fall through to a path that drops one of the flags","quality_ok":true,"anchor_id":"PF-006","decisions_status":"Active","raw_body":"\n## PF-006: A subcommand-dispatch guard that requires one flag to be absent silently drops a help-advertised flag combination by falling through to a different code path\n\n- **Area**: subcommand flag dispatch (rskim search empty-query action selection)\n- **Issue**: the standalone --ast dispatch arm was gated by blast_radius.is_none(), so when both --ast and --blast-radius were set with no text query the match fell through to run_temporal_standalone, which applies only the co-change filter and silently ignores --ast — even though help text advertises the combination as valid (AST intersect co-change)\n- **Impact**: a feature documented as supported was silently inert with no error — the worst failure mode, since the user gets plausible-looking results that omit the requested AST filter\n- **Resolution**: when a dispatch arm selects on a flag, never use a sibling-flag-absent guard (other_flag.is_none()) to disambiguate composable flags — match the primary flag unconditionally and let the arm body honor the secondary flag\n- **Status**: Active\n- **Source**: self-learning:obs_dsp4kn\n"}
{"id":"obs_xt9k2v","type":"pitfall","pattern":"A regression test that only asserts the process exits 0 is vacuous — it passes even while the bug it claims to guard is live; replace it with a real assertion (strict subset / exact-set) that fails when the bug returns","confidence":0.9,"observations":1,"first_seen":"2026-06-09T20:00:39Z","last_seen":"2026-06-09T20:00:39Z","status":"created","evidence":["session ecb261b6: prior turn states the false-confidence test was replaced with a real strict-subset intersection assertion that would actually catch the regression","commit 66b25b6 ISSUE-2: ast_plus_blast_radius_no_text_does_not_silently_drop_ast asserted only exit 0 and would pass even when --ast was silently dropped (the PF-006 bug)","commit 66b25b6: replaced with ast_blast_radius_intersection_is_applied_not_silently_dropped — proves unfiltered --ast matches BOTH files, filtered --blast-radius matches ONLY one (strict subset), and absent temporal DB degrades to full AST set","the exit-0-only test gave false confidence: it was green both before and after the fix, so it could never catch the PF-006 regression it was named to guard"],"details":"area: regression test design for CLI dispatch / filter behavior (rskim search --ast + --blast-radius); issue: a test named to guard the PF-006 silent-flag-drop bug asserted only that the process exits 0, so it passed identically whether --ast was honored or silently dropped — it was vacuous and gave false confidence, staying green across the very regression it claimed to catch; impact: a green test suite masks a live bug; the named guard provides no protection and actively misleads reviewers into believing the path is covered; resolution: a regression test must assert the discriminating observable behavior, not just a non-failing exit code — here, prove the result set is a STRICT SUBSET when the filter is applied vs the unfiltered superset (and verify graceful degradation when inputs are absent), so the test fails the moment the filter is dropped. General rule: if a test would still pass with the feature deleted, it asserts nothing.","quality_ok":true,"anchor_id":"PF-007","decisions_status":"Active","raw_body":"\n## PF-007: A regression test that only asserts the process exits 0 is vacuous — it passes even while the bug it claims to guard is live; replace it with a real assertion (strict subset / exact-set) that fails when the bug returns\n\n- **Area**: regression test design for CLI dispatch and filter behavior (rskim search --ast + --blast-radius)\n- **Issue**: a test named to guard the PF-006 silent-flag-drop bug asserted only that the process exits 0, so it passed identically whether --ast was honored or silently dropped — it was vacuous and gave false confidence, staying green across the very regression it claimed to catch\n- **Impact**: a green test suite masks a live bug, and the named guard actively misleads reviewers into believing the path is covered\n- **Resolution**: a regression test must assert the discriminating observable behavior, not just a non-failing exit code — here, prove the result set is a STRICT SUBSET when the filter is applied versus the unfiltered superset (and verify graceful degradation when inputs are absent), so the test fails the moment the filter is dropped. General rule: if a test would still pass with the feature deleted, it asserts nothing.\n- **Status**: Active\n- **Source**: self-learning:obs_xt9k2v\n"}
{"id":"obs_knstal","type":"pitfall","pattern":"Feature-knowledge files (KNOWLEDGE.md, index.json) go stale after a rename refactor, leaving broken import-path references","details":"Feature-knowledge files (KNOWLEDGE.md, index.json) go stale after a rename refactor, leaving broken import-path references","anchor_id":"PF-008","decisions_status":"Active","status":"created","raw_body":"\n## PF-008: Feature-knowledge files (KNOWLEDGE.md, index.json) go stale after a rename refactor, leaving broken import-path references\n\n- **Area**: .devflow feature-knowledge maintenance after refactors\n- **Issue**: a module or symbol rename (test_support to test_utils, PR #279) propagated through 77 compiled .rs files but left build-parsers KNOWLEDGE.md and index.json referencing the old name and a now-broken crate::cmd::test_support import path, because doc and metadata files are not type-checked by cargo\n- **Impact**: feature knowledge surfaced to future agents points at non-existent symbols, eroding trust in the pre-computed context and causing wasted exploration\n- **Resolution**: when a rename refactor touches symbols named in feature knowledge, grep KNOWLEDGE.md and index.json for the old identifier with word boundaries and update referencedFiles and import paths in the same PR — treat feature-knowledge drift as part of the refactor blast radius\n- **Status**: Active\n- **Source**: self-learning:obs_knstal\n"}
{"id":"obs_clpat0","type":"pitfall","pattern":"cargo clippy -p <crate> does not compile a binary crate's #[cfg(test)] modules, so warnings in test code pass per-crate local checks and only surface later under --all-targets or on CI","details":"area: Rust lint workflow for the cargo workspace (clippy on binary-crate test modules); issue: cargo clippy -p <crate> (and a bare cargo clippy without --all-targets) does NOT compile a binary crate's #[cfg(test)] modules, so any clippy warnings living in those test files — unused imports, doc overindentation, etc. — are not reported by the per-crate local check; they slip through and only surface later when a merge or CI run uses --all-targets, turning a clean local lint into a surprise warning batch at integration time; impact: under a zero-warnings -D warnings policy this means a locally-green branch can fail lint on merge/CI, wasting a round trip and eroding trust that local checks match the gate; resolution: always run workspace lint as cargo clippy --workspace --all-targets -- -D warnings (not cargo clippy -p <crate>) so binary-crate test modules are compiled and linted; treat the per-crate clippy as insufficient for the pre-merge gate","anchor_id":"PF-009","decisions_status":"Active"}
