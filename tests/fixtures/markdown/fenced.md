# Fenced fixture

## Section 1
Prose line 4 with `inline code` that is not a fence.
Prose line 5 with `inline code` that is not a fence.

Prose line 7 with `inline code` that is not a fence.
Prose line 8 with `inline code` that is not a fence.
## Section 2
Prose line 10 with `inline code` that is not a fence.
Prose line 11 with `inline code` that is not a fence.

Prose line 13 with `inline code` that is not a fence.
Prose line 14 with `inline code` that is not a fence.
## Section 3
Prose line 16 with `inline code` that is not a fence.
Prose line 17 with `inline code` that is not a fence.

Prose line 19 with `inline code` that is not a fence.
Prose line 20 with `inline code` that is not a fence.
## Section 4
Prose line 22 with `inline code` that is not a fence.
Prose line 23 with `inline code` that is not a fence.

Prose line 25 with `inline code` that is not a fence.
Prose line 26 with `inline code` that is not a fence.
## Section 5
Prose line 28 with `inline code` that is not a fence.
Prose line 29 with `inline code` that is not a fence.

Prose line 31 with `inline code` that is not a fence.
Prose line 32 with `inline code` that is not a fence.
```typescript
export function fenced(): void {
  const a = 1;
  const b = 2;
  return;
}

```


## Tail section 43
Tail prose line 44.
Tail prose line 45.
Tail prose line 46.
Tail prose line 47.

## Tail section 49
Tail prose line 50.
Tail prose line 51.
Tail prose line 52.
Tail prose line 53.

## Tail section 55
Tail prose line 56.
Tail prose line 57.
Tail prose line 58.
Tail prose line 59.

